{
  "name": "@ax-crew/mcp-google",
  "version": "1.0.0",
  "description": "MCP Server for Gmail, Google Drive, Google Sheets and Calendar",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run clean && npm run build",
    "start": "ts-node src/server.ts",
    "dev": "ts-node-dev --respawn src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
} 